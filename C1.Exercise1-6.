#include <stdio.h>
 
 int main()
 {       
     int c;
     c = getchar() != EOF ;
  printf("c = getchar() != EOF: %d\n", c );   /* Spoiler el valor de getchar() != EOF es 1 */
  
   return 0;
 }         
